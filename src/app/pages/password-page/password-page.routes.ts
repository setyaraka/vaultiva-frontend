import { Routes } from '@angular/router';
import { PasswordPageComponent } from './password-page.component';

export const PASSWORD_PAGE_ROUTES: Routes = [
  {
    path: '',
    component: PasswordPageComponent,
  }
];