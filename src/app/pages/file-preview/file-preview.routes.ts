import { Routes } from '@angular/router';
import { FilePreviewComponent } from './file-preview.component';

export const FILE_PREVIEW_ROUTES: Routes = [
  {
    path: '',
    component: FilePreviewComponent,
  }
];