import { Routes } from '@angular/router';
import { DemoPreviewComponent } from './demo-preview.component';

export const DEMO_PREVIEW_ROUTES: Routes = [
  {
    path: '',
    component: DemoPreviewComponent,
  }
];